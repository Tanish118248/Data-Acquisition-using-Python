<!-- # PDF Data Extraction Project

## Overview
This project fetches PDFs, extracts text and metadata, cleans it, and stores the structured data in JSON and CSV formats.

## Folder Structure
- data/
  - raw_pdfs/
  - papers_dataset.json
  - papers_dataset.csv
- scripts/
  - fetch_pdfs.py
  - extract_text.py
  - clean_data.py
  - main.py

## Setup
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the entire pipeline:
   ```bash
   python scripts/main.py
   ``` -->
